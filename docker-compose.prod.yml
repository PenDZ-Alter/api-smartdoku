version: '3.8'

services:
  app:
    build:
      context: .
      target: production
      args:
        APP_ENV: production
    ports:
      - "${PORT}:${PORT}"
    environment:
      RUN_MIGRATIONS: "true"
    env_file:
      - .env.production
      - prisma/.env