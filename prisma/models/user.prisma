model User {
  id            String      @id @default(cuid())
  name          String
  username      String  
  email         String      @unique
  address       String?     @db.Text
  password      String
  bidang        Disposisi?
  phone_number  BigInt?
  role          Role        @default(USER)
}