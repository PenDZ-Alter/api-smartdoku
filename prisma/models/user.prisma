model User {
  id            Int       @id @default(autoincrement())
  name          String
  username      String  
  email         String    @unique
  address       String?   @db.Text
  password      String
  bidang        Bidang?
  phone_number  BigInt?
  role          Role      @default(USER)
}