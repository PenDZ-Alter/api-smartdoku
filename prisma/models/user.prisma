model User {
  id            String      @id @default(cuid())
  name          String
  username      String      @unique
  email         String      @unique
  address       String?     @db.Text
  password      String
  bidang        Disposisi?
  phone_number  String?
  role          Role        @default(USER)
}